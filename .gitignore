*.zip

build
node_modules

# Local full-res asset backup (created during image optimization; not committed)
/assets-originals/

# Playwright MCP scratch output (screenshots, snapshots, console logs)
.playwright-mcp/
